<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">

    <changeSet id="20250725-update-shift-status" author="josh">
        <!-- First add the new status column -->
        <sql>
            ALTER TABLE shift ADD COLUMN IF NOT EXISTS status varchar(20);
            UPDATE shift SET status = CASE WHEN is_active = true THEN 'ACTIVE' ELSE 'INACTIVE' END;
            ALTER TABLE shift ALTER COLUMN status SET NOT NULL;
            ALTER TABLE shift DROP COLUMN is_active;
        </sql>
    </changeSet>

</databaseChangeLog> 